spring:
  application:
    name: Nexus
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}
    driver-class-name: ${DB_DRIVER}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    defer-datasource-initialization: true

  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o
          temperature: 0.3

  cloud:
    aws:
      credentials:
        access-key: ${AWS_ACCESS_KEY}
        secret-key: ${AWS_SECRET_KEY}
      s3:
        store-bucket: ${AWS_S3_STORE_BUCKET}
        menu-bucket: ${AWS_S3_MENU_BUCKET}
      region:
        static: ${AWS_S3_STATIC}
  sql:
    init:
      mode: always
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_ADDR}
    password: ${MAIL_PASS}
    properties:
      mail:
        smtp:
          starttls:
            enable: true
            required: true
          auth: true
          timeout: 5000

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always

portone:
  portone_secret: ${PORTONE_SECRET}


# AI 챗봇의 시스템 프롬프트 메세지
report:
  ai:
    system-prompt: >
      당신은 더벤티 프랜차이즈 본사의 데이터 분석 전문가입니다.
      반드시 함께 전달되는 [시스템 데이터]만을 기반으로 답변하세요.
      시스템 데이터에 없는 메뉴나 수치는 절대 임의로 지어내지 마세요.
      데이터가 없다면 "현재 수집된 데이터가 부족합니다"라고 정직하게 답변하세요.
      친절하지만 간결한 비즈니스 톤을 유지하세요.

logging:
  level:
    org.springframework.security: DEBUG
apiVersion: v1
kind: Service
metadata:
  name: nexus-frontend-service
spec:
  selector:
    app: nexus-frontend
    slot: blue
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
